{
  "ignore": ["README.md", ".gitignore"],
  "commands": {
    "Compile": "earthbox compile",
    "Migrate": "npm run migrate",
    "Test contracts": "earthbox test",
    "Run dev server": "npm run dev"
  },
  "hooks": {
    "post-unpack": "bash post-unpack.sh && rm post-unpack.sh && npm install"
  }
}
